/**
 * Common interface for points. Both Point and ObservablePoint implement it
 * @category maths
 * @standard
 */
export interface PointData
{
    /** X coord */
    x: number;

    /** Y coord */
    y: number;
}
